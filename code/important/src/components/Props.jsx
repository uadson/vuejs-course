const Props = (props) => {

    return (
        <div>
            <h1>Props</h1>
            <div>
                <h3>Show the user name with props: <u>{props.username}</u></h3>
            </div>
        </div>
    )
}

export default Props